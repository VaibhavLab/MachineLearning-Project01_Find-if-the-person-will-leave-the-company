Missing values
→ preprocessing

Categorical columns
→ One-Hot Encoding

Different numeric scales
→ scaling where needed

Binary target
→ classification

Baseline
→ majority-class prediction

Logistic Regression
→ probabilities + sigmoid + threshold

Decision Tree
→ splitting + overfitting

Random Forest
→ ensemble + better generalization

Precision / Recall / F1
→ proper classification evaluation

Train vs Test performance
→ detect overfitting

Cross-validation
→ verify model stability

Feature importance
→ understand what influences churn