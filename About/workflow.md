Load dataset
→ Understand every column
→ Define X and y
→ Check missing values
→ Remove useless ID columns
→ Train/Test split
→ Fit preprocessing ONLY on training data
→ Handle missing values
→ One-Hot Encode categorical columns
→ Scale numeric columns when required
→ Create baseline
→ Logistic Regression
→ Decision Tree
→ Random Forest
→ Compare Accuracy / Precision / Recall / F1
→ Compare train score vs test score
→ Understand overfitting
→ Learn Bias–Variance here
→ Use Cross-Validation
→ Tune only a few meaningful hyperparameters
→ Select final model
→ Evaluate once on final test data
→ Save model
→ Load model
→ Predict churn for a new customer
→ Write README